# Top-Up Kredit Mid-Period Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** User pro/hengker AKTIF bisa membeli top-up 15 kredit / Rp 20.000 di tengah periode — kredit masuk kolam yang sama (aditif, hangus bareng), masa aktif tidak bergeser, dibatasi cap per periode.

**Architecture:** Order top-up = row `payments` dengan `plan = "topup-15"` + jalur grant ADITIF baru (`applyTopUpSuccess`) yang dirouting lewat dispatcher tunggal `applyOrderSuccess` (kedua pemanggil lama — webhook & `syncPaymentStatus` — diganti ke dispatcher). Pra-checkout ketat (eligibility `active_paid` + cap), grant-time toleran overshoot maks 1 SKU tapi ketat menolak jika state bukan `active_paid`. Zero migration skema, `src/lib/credits.ts` tidak disentuh.

**Tech Stack:** TanStack Start (file-based routing), Drizzle ORM + PostgreSQL 17, Midtrans Snap (existing), Vitest, Biome, pnpm.

**Spec:** `docs/superpowers/specs/2026-08-25-topup-design.md` — baca dulu sebelum eksekusi.

## Global Constraints

- TanStack Start file-based routing di `src/routes/`. Bukan Next.js. Jangan import `next/*`.
- Server-only (`db`, SDK eksternal): dynamic import di dalam fungsi pada modul yang di-import client; API routes boleh top-level `import { db }` (pola existing `create.ts`).
- **`src/lib/billing.ts` HARUS tetap pure (zero import `@/db`)** — logika keputusan baru hidup di sini.
- **`src/lib/credits.ts` TIDAK BOLEH DIUBAH** — kredit top-up masuk kolam sama; hot path bekerja apa adanya.
- Konstanta SKU hidup di `src/lib/constants.ts`; TIDAK ada magic number harga/kredit tersebar.
- Idempotensi pembayaran: `SELECT … FOR UPDATE` + bail `status === "success"` + mark-success-LAST dipertahankan.
- UI copy Bahasa Indonesia; istilah teknis English. Commit message English conventional.
- Verify commands per task: `pnpm exec tsc --noEmit` (types), `pnpm check` (biome), `pnpm vitest run <file>` (unit). Suite penuh: `pnpm vitest run`.
- Baseline known failure: `flow-progress.test.ts` GAGAL 1 test di main (pre-existing, commit `5073500`) — jumlah failure tidak boleh bertambah.
- YAGNI: tanpa auto-charge, proration, pool kedua, refund otomatis.

---

### Task 1: Pure core — konstanta SKU + helper billing + unit test

**Files:**
- Modify: `src/lib/constants.ts` (tambah di akhir file, setelah `REMINDER_SCHEDULE_DAYS`)
- Modify: `src/lib/billing.ts` (tambah 2 fungsi di akhir file)
- Test: `src/lib/billing.test.ts` (append 2 describe)

**Interfaces:**
- Consumes: `EffectiveSubscription`, `resolveSubscriptionState`, `PLAN_CREDITS`, `type Plan` (semua sudah ada di file terkait).
- Produces (dipakai Task 2, 4, 5, 6):
  - `TOPUP_SKU: { readonly id: "topup-15"; readonly credits: 15; readonly priceIdr: 20000 }` dari `@/lib/constants`
  - `canPurchaseTopUp(eff: EffectiveSubscription): boolean` dari `@/lib/billing`
  - `remainingTopUpQuota(params: { plan: Plan; usedThisPeriod: number }): number` dari `@/lib/billing`

- [ ] **Step 1: Tulis test yang gagal**

Append di akhir `src/lib/billing.test.ts`. Tambahkan `canPurchaseTopUp` dan `remainingTopUpQuota` ke import statement existing di baris 2-9:

```ts
import {
	addDays,
	canPurchaseTopUp,
	computeFreeRolloverPeriod,
	computePurchaseGrant,
	isFreeRolloverDue,
	remainingTopUpQuota,
	resolveSubscriptionState,
	type SubscriptionRowLike,
} from "./billing";
```

Lalu append describe baru (fungsi `row()` dan konstanta `NOW` sudah didefinisikan di atas file):

```ts
describe("canPurchaseTopUp", () => {
	it("true only for active_paid", () => {
		expect(canPurchaseTopUp(resolveSubscriptionState(row(), NOW))).toBe(true);
	});

	it("false for paused (expired period)", () => {
		const eff = resolveSubscriptionState(
			row({ currentPeriodEnd: addDays(NOW, -3) }),
			NOW,
		);
		expect(canPurchaseTopUp(eff)).toBe(false);
	});

	it("false for legacy_grandfathered (no running period)", () => {
		const eff = resolveSubscriptionState(row({ currentPeriodEnd: null }), NOW);
		expect(canPurchaseTopUp(eff)).toBe(false);
	});

	it("false for free_active, including cancelled paid rows", () => {
		expect(
			canPurchaseTopUp(resolveSubscriptionState(row({ plan: "free" }), NOW)),
		).toBe(false);
		expect(
			canPurchaseTopUp(resolveSubscriptionState(row({ cancelledAt: NOW }), NOW)),
		).toBe(false);
	});
});

describe("remainingTopUpQuota", () => {
	it("returns full allocation when nothing used this period", () => {
		expect(remainingTopUpQuota({ plan: "pro", usedThisPeriod: 0 })).toBe(30);
	});

	it("subtracts partial usage (hengker)", () => {
		expect(remainingTopUpQuota({ plan: "hengker", usedThisPeriod: 45 })).toBe(
			60,
		);
	});

	it("returns 0 when cap reached exactly", () => {
		expect(remainingTopUpQuota({ plan: "pro", usedThisPeriod: 30 })).toBe(0);
	});

	it("clamps over-cap history to 0, never negative", () => {
		expect(remainingTopUpQuota({ plan: "pro", usedThisPeriod: 45 })).toBe(0);
	});
});
```

- [ ] **Step 2: Jalankan test, pastikan GAGAL**

Run: `pnpm vitest run src/lib/billing.test.ts`
Expected: FAIL — `SyntaxError: The requested module './billing' does not provide an export named 'canPurchaseTopUp'`

- [ ] **Step 3: Tambahkan konstanta SKU di `src/lib/constants.ts`**

Append di akhir file:

```ts
// === Credit top-up (mid-period purchase) ===
// Single universal SKU: bought by ACTIVE Pro/Hengker subscribers only
// (state active_paid). Credits join the SAME pool as the monthly allocation
// (shared credits/creditsUsed) and are forfeited together at period end.
// Buying NEVER extends the current period. Anti-undercut cap per period =
// PLAN_CREDITS[plan]; tracked from successful topup payments within
// [current_period_start, current_period_end] (spec topup-design §4).
export const TOPUP_SKU = {
	id: "topup-15",
	credits: 15,
	priceIdr: 20000,
} as const;
```

- [ ] **Step 4: Tambahkan 2 helper pure di `src/lib/billing.ts`**

Append di akhir file:

```ts
/** Top-up purchase is exclusive to an ACTIVE paid subscription (spec §1). */
export function canPurchaseTopUp(eff: EffectiveSubscription): boolean {
	return eff.state === "active_paid";
}

/**
 * Remaining top-up allowance for the CURRENT period (anti-undercut cap):
 * plan allocation minus successful top-up credits this period. Clamps to 0
 * so over-cap histories can never enable another buy (spec §4).
 */
export function remainingTopUpQuota(params: {
	plan: Plan;
	usedThisPeriod: number;
}): number {
	return Math.max(0, PLAN_CREDITS[params.plan] - params.usedThisPeriod);
}
```

- [ ] **Step 5: Jalankan test, pastikan PASS**

Run: `pnpm vitest run src/lib/billing.test.ts`
Expected: PASS semua (test lama + 8 assertion baru)

- [ ] **Step 6: Verify types**

Run: `pnpm exec tsc --noEmit`
Expected: 0 error.

- [ ] **Step 7: Commit**

```bash
git add src/lib/constants.ts src/lib/billing.ts src/lib/billing.test.ts
git commit -m "feat(billing): top-up SKU constant and pure purchase/quota helpers"
```

---

### Task 2: Service layer — routing predicate, hitung kuota, grant aditif, dispatcher

**Files:**
- Modify: `src/lib/services/payment-service.ts`
- Test: `src/lib/services/payment-service.test.ts` (append)

**Interfaces:**
- Consumes: `TOPUP_SKU` (Task 1), `resolveSubscriptionState` dari `@/lib/billing` (existing).
- Produces (dipakai Task 3, 4):
  - `isTopUpOrder(planValue: string | null | undefined): boolean` — murni
  - `getTopUpCreditsUsedThisPeriod(userId: string): Promise<number>` — total kredit top-up sukses dalam periode berjalan (0 jika tak ada periode)
  - `applyTopUpSuccess(orderId: string): Promise<{ plan: Plan } | null>` — grant ADDITIF, re-validasi ketat `active_paid`, idempotent, return `{ plan }` plan efektif user
  - `applyOrderSuccess(orderId: string): Promise<{ plan: Plan } | null>` — dispatcher: route by `payments.plan`

- [ ] **Step 1: Tulis test routing predicate yang gagal**

Append di akhir `src/lib/services/payment-service.test.ts`. Ganti dua import di atas file menjadi:

```ts
import { describe, expect, it } from "vitest";
import { TOPUP_SKU } from "@/lib/constants";
import {
	creditsForPlan,
	isTopUpOrder,
	planFromAmount,
} from "./payment-service";
```

Lalu append describe baru:

```ts
describe("top-up order routing", () => {
	it("recognizes the top-up SKU id", () => {
		expect(isTopUpOrder(TOPUP_SKU.id)).toBe(true);
	});

	it("routes plan purchases and unknown values to the legacy path", () => {
		expect(isTopUpOrder("pro")).toBe(false);
		expect(isTopUpOrder("hengker")).toBe(false);
		expect(isTopUpOrder(null)).toBe(false);
		expect(isTopUpOrder(undefined)).toBe(false);
	});
});

describe("monthly mapper ignores top-up amounts (regression)", () => {
	it("throws for the top-up price so stray orders fail loudly upstream", () => {
		expect(() => planFromAmount(TOPUP_SKU.priceIdr)).toThrow(
			/does not match any plan price/,
		);
	});
});
```

- [ ] **Step 2: Jalankan test, pastikan GAGAL**

Run: `pnpm vitest run src/lib/services/payment-service.test.ts`
Expected: FAIL — export `isTopUpOrder` tidak ada

- [ ] **Step 3: Implementasi di `src/lib/services/payment-service.ts`**

Ganti import blok atas file (baris 14-17) menjadi:

```ts
import { and, desc, eq, gte, lte, sql } from "drizzle-orm";
import { computePurchaseGrant, resolveSubscriptionState } from "@/lib/billing";
import { TOPUP_SKU } from "@/lib/constants";
import { prdFyPlans } from "@/lib/pricing-data";
import type { Plan } from "@/types/database";
```

Update docstring atas file — ganti paragraf `Credit model:` dengan:

```ts
 * Credit model: monthly subscription. A plan purchase SETS the plan's credit
 * allocation and (re)writes the billing period via computePurchaseGrant
 * (lib/billing.ts). A top-up order (payments.plan === TOPUP_SKU.id) instead
 * ADDS credits to the same pool and never touches plan/period columns.
 * Rows whose current_period_end stays NULL are legacy one-time purchases
 * honored until their credits run out.
```

Tambahkan `isTopUpOrder` setelah `creditsForPlan`:

```ts
/** Routing discriminator (spec §3): key off the stored SKU, not order prefixes. */
export function isTopUpOrder(planValue: string | null | undefined): boolean {
	return planValue === TOPUP_SKU.id;
}
```

Tambahkan `getTopUpCreditsUsedThisPeriod` setelahnya (dynamic-import pattern sesuai gaya file):

```ts
/**
 * Total successful top-up credits bought within the CURRENT billing period
 * (spec §4 anti-undercut cap). Returns 0 when there is no bounded period
 * (missing sub row or NULL bounds — e.g. legacy grandfathered).
 */
export async function getTopUpCreditsUsedThisPeriod(
	userId: string,
): Promise<number> {
	const { db } = await import("@/db");
	const { payments, subscriptions } = await import("@/db/schema");

	const [sub] = await db
		.select({
			start: subscriptions.currentPeriodStart,
			end: subscriptions.currentPeriodEnd,
		})
		.from(subscriptions)
		.where(eq(subscriptions.userId, userId))
		.orderBy(desc(subscriptions.createdAt))
		.limit(1);
	if (!sub?.start || !sub?.end) return 0;

	const [row] = await db
		.select({ n: sql<number>`count(*)::int` })
		.from(payments)
		.where(
			and(
				eq(payments.userId, userId),
				eq(payments.status, "success"),
				eq(payments.plan, TOPUP_SKU.id),
				gte(payments.createdAt, sub.start),
				lte(payments.createdAt, sub.end),
			),
		);
	return (row?.n ?? 0) * TOPUP_SKU.credits;
}
```

Tambahkan `applyTopUpSuccess` setelah `applyPaymentSuccess`:

```ts
/**
 * Grants a top-up AFTER Midtrans confirms (spec §5.3). ADDITIVE: bumps
 * `credits` by TOPUP_SKU.credits and touches NOTHING else on the
 * subscription row (plan/status/period/cancelled_at/reminder_count stay).
 *
 * - Idempotent: FOR UPDATE + bail when already success (retry-safe).
 * - Strict eligibility AT GRANT TIME: if the effective state drifted away
 *   from active_paid while checkout was open (e.g. period expired seconds
 *   before the webhook), the grant is REJECTED, the payment is marked failed
 *   with a midtransResponse note, and money is refunded manually via the
 *   Midtrans dashboard (spec §9). Granting into a paused account would burn
 *   the buyer's money silently.
 * - Cap is enforced strictly at checkout creation, tolerantly here (spec
 *   §5.3 point 4): a paid order is always honored; worst-case race overshoot
 *   is one SKU.
 */
export async function applyTopUpSuccess(orderId: string) {
	const { db } = await import("@/db");
	const { payments, subscriptions } = await import("@/db/schema");

	return db.transaction(async (tx) => {
		const [payment] = await tx
			.select()
			.from(payments)
			.where(eq(payments.orderId, orderId))
			.for("update")
			.limit(1);
		if (!payment) return null;
		if (payment.status === "success") {
			// Idempotent replay: report the current effective plan so the
			// caller contract ({ plan }) matches applyPaymentSuccess.
			const [sub] = await tx
				.select({ plan: subscriptions.plan })
				.from(subscriptions)
				.where(eq(subscriptions.userId, payment.userId))
				.orderBy(desc(subscriptions.createdAt))
				.limit(1);
			return { plan: (sub?.plan ?? "pro") as Plan };
		}

		// Defensive: only genuine top-up orders may take this path.
		if (payment.plan !== TOPUP_SKU.id || payment.amount !== TOPUP_SKU.priceIdr)
			return null;

		const now = new Date();
		const [sub] = await tx
			.select({
				id: subscriptions.id,
				plan: subscriptions.plan,
				status: subscriptions.status,
				credits: subscriptions.credits,
				creditsUsed: subscriptions.creditsUsed,
				currentPeriodStart: subscriptions.currentPeriodStart,
				currentPeriodEnd: subscriptions.currentPeriodEnd,
				cancelledAt: subscriptions.cancelledAt,
			})
			.from(subscriptions)
			.where(eq(subscriptions.userId, payment.userId))
			.orderBy(desc(subscriptions.createdAt))
			.limit(1);

		const eff = resolveSubscriptionState(sub, now);
		if (eff.state !== "active_paid" || !sub) {
			await tx
				.update(payments)
				.set({
					status: "failed",
					midtransResponse: {
						...(typeof payment.midtransResponse === "object" &&
						payment.midtransResponse !== null
							? payment.midtransResponse
							: {}),
						topupRejected: "not_active_paid",
					},
					updatedAt: now,
				})
				.where(eq(payments.orderId, orderId));
			console.error(
				`[topup] grant rejected for ${orderId}: subscription state ${eff.state}`,
			);
			return null;
		}

		await tx
			.update(subscriptions)
			.set({
				credits: sql`${subscriptions.credits} + ${TOPUP_SKU.credits}`,
				updatedAt: now,
			})
			.where(eq(subscriptions.id, sub.id));

		// Mark success LAST so a retry re-runs the grant if it died mid-way.
		await tx
			.update(payments)
			.set({ status: "success", updatedAt: now })
			.where(eq(payments.orderId, orderId));

		const [after] = await tx
			.select({ plan: subscriptions.plan })
			.from(subscriptions)
			.where(eq(subscriptions.userId, payment.userId))
			.orderBy(desc(subscriptions.createdAt))
			.limit(1);
		return { plan: (after?.plan ?? "pro") as Plan };
	});
}
```

Tambahkan dispatcher `applyOrderSuccess` di akhir file:

```ts
/**
 * Single entry point for "money arrived, apply it" (spec §5.4). Routes by the
 * stored SKU so BOTH completion paths (payments webhook + syncPaymentStatus)
 * share one decision. Unknown orders fall through to the plan path, whose
 * planFromAmount throws loudly — same semantics as before this feature.
 */
export async function applyOrderSuccess(orderId: string) {
	const { db } = await import("@/db");
	const { payments } = await import("@/db/schema");
	const [row] = await db
		.select({ plan: payments.plan })
		.from(payments)
		.where(eq(payments.orderId, orderId))
		.limit(1);
	if (!row) return applyPaymentSuccess(orderId);
	return isTopUpOrder(row.plan)
		? applyTopUpSuccess(orderId)
		: applyPaymentSuccess(orderId);
}
```

- [ ] **Step 4: Jalankan test, pastikan PASS**

Run: `pnpm vitest run src/lib/services/payment-service.test.ts src/lib/billing.test.ts`
Expected: PASS semua (termasuk regression `planFromAmount` throw untuk 20000)

- [ ] **Step 5: Verify types + lint**

Run: `pnpm exec tsc --noEmit; if ($?) { pnpm check }`
Expected: keduanya bersih.

- [ ] **Step 6: Commit**

```bash
git add src/lib/services/payment-service.ts src/lib/services/payment-service.test.ts
git commit -m "feat(payments): top-up routing dispatcher with additive credit grant"
```

---

### Task 3: Ganti kedua pemanggil ke dispatcher `applyOrderSuccess`

**Files:**
- Modify: `src/routes/api/payments/webhook.ts:6` dan `:48`
- Modify: `src/app/actions/payment.ts:21-23` dan `:62`

**Interfaces:**
- Consumes: `applyOrderSuccess(orderId: string): Promise<{ plan: Plan } | null>` (Task 2).
- Produces: perilaku runtime baru (order top-up selesai diproses di kedua jalur penyelesaian). Tidak ada signature berubah.

- [ ] **Step 1: webhook.ts**

Ganti baris 6:

```ts
import { applyOrderSuccess } from "@/lib/services/payment-service";
```

Ganti baris 48:

```ts
					try {
						await applyOrderSuccess(order_id);
```

(blok try/catch di sekitarnya tidak berubah)

- [ ] **Step 2: syncPaymentStatus di `src/app/actions/payment.ts`**

Ganti dynamic import (baris 21-23):

```ts
		const { applyOrderSuccess } = await import(
			"@/lib/services/payment-service"
		);
```

Ganti pemanggilan (baris 62):

```ts
			const result = await applyOrderSuccess(orderId);
```

Catatan: cabang `payment.status === "success"` lebih awal (baris 33-45) membaca `subscriptions.plan` langsung — untuk order top-up hasilnya plan efektif user (pro/hengker), kontrak respons tidak berubah.

- [ ] **Step 3: Verify types + suite**

Run: `pnpm exec tsc --noEmit; if ($?) { pnpm vitest run }`
Expected: tsc bersih; suite PASS (baseline 1 failure `flow-progress.test.ts` tidak bertambah).

- [ ] **Step 4: Commit**

```bash
git add src/routes/api/payments/webhook.ts src/app/actions/payment.ts
git commit -m "feat(payments): route settlement completions through applyOrderSuccess"
```

---

### Task 4: Endpoint create — cabang top-up dengan pra-checkout ketat

**Files:**
- Modify: `src/routes/api/payments/create.ts` (rewrite handler POST)

**Interfaces:**
- Consumes: `TOPUP_SKU` (Task 1); `canPurchaseTopUp`, `remainingTopUpQuota` (Task 1); `getTopUpCreditsUsedThisPeriod` (Task 2); `getCreditBalance` dari `@/lib/credits` (existing, tidak diubah); `PLAN_CREDITS` dari `@/types/database`.
- Produces: `POST /api/payments/create` menerima `planId: "topup-15"` → 403/400 bila gagal gate, selain itu order `TOPUP-*` + Snap redirect seperti biasa. Respons sukses bentuknya identik: `{ redirect_url, token }`.

- [ ] **Step 1: Rewrite `src/routes/api/payments/create.ts`**

Ganti SELURUH isi file menjadi:

```ts
import { randomBytes } from "node:crypto";
import { createFileRoute } from "@tanstack/react-router";
import { getRequestHeaders } from "@tanstack/react-start/server";
import { and, eq, lt } from "drizzle-orm";
import { db } from "@/db";
import { payments } from "@/db/schema";
import { canPurchaseTopUp, remainingTopUpQuota } from "@/lib/billing";
import { TOPUP_SKU } from "@/lib/constants";
import { isValidHistoryUrl } from "@/lib/flow-progress";
import { getCreditBalance } from "@/lib/credits";
import { prdFyPlans } from "@/lib/pricing-data";
import { getTopUpCreditsUsedThisPeriod } from "@/lib/services/payment-service";
import { requireUser } from "@/lib/session";
import { PLAN_CREDITS } from "@/types/database";

const ALLOWED_ORIGINS = [
	"https://novaplanai.vercel.app",
	"https://novaplanai-git-main-ghazy-alghifaris-projects.vercel.app",
	"http://localhost:3000",
];

export const Route = createFileRoute("/api/payments/create")({
	server: {
		handlers: {
			POST: async ({ request }: { request: Request }) => {
				const user = await requireUser(getRequestHeaders());
				const { planId, returnUrl, projectId } = (await request.json()) as {
					planId: string;
					returnUrl?: string;
					projectId?: string;
				};

				// Two product kinds share this endpoint (spec §6.1): a monthly plan
				// subscription (SET semantics) and a mid-period credit top-up
				// (additive credits, period untouched). Gates differ accordingly.
				const isTopUp = planId === TOPUP_SKU.id;

				let amount: number;
				let planCredits: number;
				let itemLabel: string;

				if (isTopUp) {
					// STRICT gates at checkout time (spec §5.3 point 4): eligibility
					// and quota are enforced HERE; grant time stays tolerant.
					const balance = await getCreditBalance(user.id);
					if (!canPurchaseTopUp(balance)) {
						return Response.json(
							{
								error:
									"Top up hanya tersedia untuk langganan Pro/Hengker yang sedang aktif.",
							},
							{ status: 403 },
						);
					}
					const used = await getTopUpCreditsUsedThisPeriod(user.id);
					if (
						remainingTopUpQuota({ plan: balance.plan, usedThisPeriod: used }) <
						TOPUP_SKU.credits
					) {
						return Response.json(
							{
								error: `Kuota top-up periode ini sudah habis (maksimal ${PLAN_CREDITS[balance.plan]} kredit). Kuota reset saat periode berikutnya.`,
							},
							{ status: 400 },
						);
					}
					amount = TOPUP_SKU.priceIdr;
					planCredits = TOPUP_SKU.credits;
					itemLabel = `Top Up ${TOPUP_SKU.credits} Kredit PrdFy`;
				} else {
					const plan = prdFyPlans.find((p) => p.id === planId);
					if (!plan)
						return Response.json(
							{ error: "Plan tidak ditemukan." },
							{ status: 404 },
						);
					if (plan.price === 0)
						return Response.json(
							{ error: "Plan gratis tidak memerlukan pembayaran." },
							{ status: 400 },
						);

					// ponytail: no plan-hierarchy guard - buying the same or a lower
					// tier again is a legitimate renewal/switch (SET semantics).
					amount = plan.price;
					planCredits = plan.credits;
					itemLabel = `Paket ${plan.name} - ${plan.credits} kredit/bulan`;
				}

				// Clean up stale pending payments for this user (>5 min) before
				// creating a new one. Prevents stacking abandoned checkouts.
				const fiveMinAgo = new Date(Date.now() - 5 * 60 * 1000);
				await db
					.update(payments)
					.set({ status: "failed" })
					.where(
						and(
							eq(payments.userId, user.id),
							eq(payments.status, "pending"),
							lt(payments.createdAt, fiveMinAgo),
						),
					);

				const orderId = `${isTopUp ? "TOPUP" : "ORDER"}-${Date.now()}-${randomBytes(4).toString("hex")}`;
				await db.insert(payments).values({
					id: crypto.randomUUID(),
					userId: user.id,
					orderId,
					// Stored SKU doubles as the completion router (see
					// applyOrderSuccess) and the per-period cap counter input.
					plan: isTopUp ? TOPUP_SKU.id : planId,
					amount,
					status: "pending",
				});

				const origin = request.headers.get("origin") || "";
				const safeOrigin = ALLOWED_ORIGINS.includes(origin)
					? origin
					: ALLOWED_ORIGINS[0];
				const serverKey = process.env.MIDTRANS_SERVER_KEY_SANDBOX || "";
				const authString = Buffer.from(`${serverKey}:`).toString("base64");

				const parameters = {
					transaction_details: { order_id: orderId, gross_amount: amount },
					customer_details: {
						first_name: user.name || "Customer",
						email: user.email,
					},
					item_details: [
						{
							id: planId,
							price: amount,
							quantity: 1,
							name: itemLabel,
						},
					],
					custom_field1: planId,
					custom_field2: String(planCredits),
					custom_field3: user.id,
					callbacks: {
						finish: (() => {
							const finishPath =
								returnUrl &&
								projectId &&
								isValidHistoryUrl(returnUrl, projectId)
									? returnUrl
									: "/pricing";
							return `${safeOrigin}${finishPath}${finishPath.includes("?") ? "&" : "?"}payment=success&order_id=${orderId}`;
						})(),
					},
				};

				try {
					const response = await fetch(
						"https://app.sandbox.midtrans.com/snap/v1/transactions",
						{
							method: "POST",
							headers: {
								"Content-Type": "application/json",
								Accept: "application/json",
								Authorization: `Basic ${authString}`,
								"X-Override-Notification": `${safeOrigin}/api/payments/webhook`,
							},
							body: JSON.stringify(parameters),
						},
					);
					if (!response.ok) throw new Error(await response.text());
					const transaction = await response.json();
					return Response.json({
						redirect_url: transaction.redirect_url,
						token: transaction.token,
					});
				} catch (error) {
					console.error("Midtrans/System Error:", error);
					await db.delete(payments).where(eq(payments.orderId, orderId));
					return Response.json(
						{
							error:
								"Terjadi kesalahan pada sistem pembayaran. Silakan coba lagi.",
						},
						{ status: 500 },
					);
				}
			},
		},
	},
});
```

- [ ] **Step 2: Verify types + lint + suite**

Run: `pnpm exec tsc --noEmit; if ($?) { pnpm check }; if ($?) { pnpm vitest run }`
Expected: semua bersih; baseline failure tidak bertambah.

- [ ] **Step 3: Commit**

```bash
git add src/routes/api/payments/create.ts
git commit -m "feat(payments): gated top-up checkout branch in create endpoint"
```

---

### Task 5: Komponen `TopUpCard` + mount di dua permukaan pricing

**Files:**
- Create: `src/components/ui/top-up-card.tsx`
- Modify: `src/components/ui/pricing-card.tsx` (return `PricingWrapper`, baris ~372-378)
- Modify: `src/components/chat/credit-exhausted-modal.tsx` (blok embedded pricing, baris ~96-106)

**Interfaces:**
- Consumes: `TOPUP_SKU` (Task 1); `useUserPlan().data.subscriptionState` (existing); pola fetch `/api/payments/create` dari `pricing-card.tsx`.
- Produces: `TopUpCard({ className }: { className?: string }): JSX.Element` — self-contained, anchor `id="topup"`, visibility diputuskan PARENT (komponen mengasumsikan harus tampil).

- [ ] **Step 1: Buat `src/components/ui/top-up-card.tsx`**

```tsx
"use client";

import { CreditCard } from "lucide-react";
import * as React from "react";
import { useUIStore } from "@/store";
import { TOPUP_SKU } from "@/lib/constants";

/**
 * Mid-period credit top-up card (spec §7). Parents decide visibility via
 * useUserPlan().subscriptionState === "active_paid"; this component assumes
 * it should render. Dark tokens (snow/fog/white-alpha) match BOTH mounting
 * surfaces: /pricing (bg-onyx) and the credit-exhausted modal (bg-obsidian).
 * Buying NEVER extends the current period — copy says so explicitly.
 */
export function TopUpCard({ className }: { className?: string }) {
	const showToast = useUIStore((s) => s.showToast);
	const [loading, setLoading] = React.useState(false);

	const handleBuy = async () => {
		setLoading(true);
		try {
			const res = await fetch("/api/payments/create", {
				method: "POST",
				headers: { "Content-Type": "application/json" },
				body: JSON.stringify({ planId: TOPUP_SKU.id }),
			});
			const data = (await res.json()) as {
				error?: string;
				redirect_url?: string;
			};
			if (!res.ok) {
				if (res.status === 401) {
					window.location.href = `/login?redirect=${encodeURIComponent(window.location.pathname)}`;
					return;
				}
				showToast(data.error || "Gagal memproses pembayaran.", "error");
				return;
			}
			if (data.redirect_url) {
				window.location.href = data.redirect_url;
			}
		} catch {
			showToast("Gagal menghubungi server.", "error");
		} finally {
			setLoading(false);
		}
	};

	return (
		<div
			id="topup"
			className={`scroll-mt-24 rounded-xl border border-white/10 bg-white/5 p-5 ${className ?? ""}`}
		>
			<div className="flex flex-wrap items-center justify-between gap-4">
				<div>
					<h3 className="flex items-center gap-2 font-inter text-base font-[510] text-snow">
						<CreditCard size={16} aria-hidden />
						Top Up Kredit
					</h3>
					<p className="mt-1 text-sm text-fog">
						{TOPUP_SKU.credits} kredit · Rp{" "}
						{TOPUP_SKU.priceIdr.toLocaleString("id-ID")}
					</p>
					<p className="mt-1 text-xs text-fog">
						Tanpa menambah masa aktif · sisa kredit ikut hangus di akhir
						periode berjalan
					</p>
				</div>
				<button
					type="button"
					disabled={loading}
					onClick={handleBuy}
					className="rounded-lg bg-white/10 px-4 py-2 text-sm font-medium text-snow transition-colors hover:bg-white/20 disabled:opacity-50"
				>
					{loading ? "Memproses..." : "Top Up Sekarang"}
				</button>
			</div>
		</div>
	);
}
```

- [ ] **Step 2: Mount di `PricingWrapper` (`src/components/ui/pricing-card.tsx`)**

Tambahkan import di blok atas file:

```tsx
import { TopUpCard } from "@/components/ui/top-up-card";
```

Ganti return statement `PricingWrapper` (baris ~372-378):

```tsx
	return (
		<>
			<PricingComponent
				plans={prdFyPlans}
				onPlanSelect={handlePlanSelect}
				currentPlan={currentPlan}
			/>
			{/* Mid-period top-up: only meaningful for ACTIVE paid subscribers
			    (spec §7). Free/paused/legacy users see nothing extra. */}
			{planData?.subscriptionState === "active_paid" && (
				<TopUpCard className="mt-6" />
			)}
		</>
	);
```

(`planData` sudah ada dari `useUserPlan()` di baris 303.)

- [ ] **Step 3: Mount di `credit-exhausted-modal.tsx`**

Tambahkan import:

```tsx
import { TopUpCard } from "@/components/ui/top-up-card";
```

Di blok `{/* Embedded pricing cards */}` (baris ~96), sisipkan SEBELUM `<PricingComponent>`:

```tsx
				{/* Embedded pricing cards */}
				<div className="px-2 pb-3">
					{/* Credits gone but period still running = ideal top-up case
					    (spec §7). Paused users get the renewal cards instead. */}
					{planData?.subscriptionState === "active_paid" && (
						<TopUpCard className="mx-2 mb-2" />
					)}
					<PricingComponent
```

(`planData` sudah ada dari `useUserPlan()` di baris 33; tag penutup `/>` milik PricingComponent tidak berubah.)

- [ ] **Step 4: Verify types + lint**

Run: `pnpm exec tsc --noEmit; if ($?) { pnpm check }`
Expected: keduanya bersih.

- [ ] **Step 5: Manual smoke (dev)**

Run: `pnpm dev` → login akun free → `/pricing`: kartu top-up TIDAK tampak. Login/ganti fixture akun pro aktif → kartu TAMPIL di bawah grid; buka project → trigger kondisi kredit habis → modal menampilkan kartu di atas grid paket. Klik tombol (sandbox) → Snap popup terbuka. Tidak ada console error.

- [ ] **Step 6: Commit**

```bash
git add src/components/ui/top-up-card.tsx src/components/ui/pricing-card.tsx src/components/chat/credit-exhausted-modal.tsx
git commit -m "feat(ui): top-up card on pricing surfaces for active paid subscribers"
```

---

### Task 6: Link top-up di halaman billing saat kredit habis

**Files:**
- Modify: `src/routes/settings/billing.tsx` (footer progress kredit, baris ~198-204)

**Interfaces:**
- Consumes: `TOPUP_SKU` (Task 1); derived state existing `isPaidPlan`, `isPaused`, `periodEndDate`, `remaining` (baris 97-118); `Link` (sudah di-import file ini).
- Produces: tautan inline menuju `/pricing#topup`, HANYA saat `active_paid` proxy (`isPaidPlan && !isPaused && periodEndDate != null`) DAN `remaining === 0`.

- [ ] **Step 1: Edit footer progress di `src/routes/settings/billing.tsx`**

Tambahkan import di blok atas file:

```tsx
import { TOPUP_SKU } from "@/lib/constants";
```

Langsung SETELAH elemen `<p>` footer yang existing (blok `{isPaused ? … : remaining > 0 ? … : "Kredit periode ini habis. Perpanjang atau tunggu reset berikutnya."}</p>` — jangan ubah ternary-nya), tambahkan paragraf baru:

```tsx
						{!isPaused &&
							remaining === 0 &&
							isPaidPlan &&
							periodEndDate != null && (
								<p className="mt-1 text-xs text-(--text-secondary)">
									<Link
										to="/pricing"
										hash="topup"
										className="font-medium underline underline-offset-2"
									>
										Atau top up {TOPUP_SKU.credits} kredit (Rp{" "}
										{(TOPUP_SKU.priceIdr as number).toLocaleString("id-ID")})
										tanpa menambah masa aktif
									</Link>
								</p>
							)}
```

Catatan kondisi: `periodEndDate != null` menyaring legacy grandfathered (periode NULL → tidak boleh lihat top-up); `!isPaused` menyaring akun pause (harus renew).

- [ ] **Step 2: Verify types + lint**

Run: `pnpm exec tsc --noEmit; if ($?) { pnpm check }`
Expected: keduanya bersih.

- [ ] **Step 3: Manual smoke (dev)**

Run: `pnpm dev` → sebagai pro aktif dengan sisa kredit 0 (fixture: set `credits_used = credits` via SQL) → `/settings/billing` → teks "Kredit periode ini habis…" diikuti link top-up; klik → mendarat di `/pricing#topup` dengan kartu terlihat. Akun legacy (periode NULL) TIDAK melihat link meski kredit 0.

- [ ] **Step 4: Commit**

```bash
git add src/routes/settings/billing.tsx
git commit -m "feat(billing-ui): inline top-up link when active-period credits run out"
```

---

### Task 7: Regresi penuh + checklist QA sandbox

**Files:**
- Tidak ada perubahan kode — verifikasi saja.

**Interfaces:**
- Consumes: seluruh task sebelumnya.
- Produces: bukti regresi hijau + checklist QA manual untuk owner.

- [ ] **Step 1: Suite penuh + types + lint + build**

Run: `pnpm exec tsc --noEmit; if ($?) { pnpm check }; if ($?) { pnpm vitest run }`
Expected: tsc bersih, biome bersih, vitest PASS dengan TEPAT 1 failure pre-existing (`flow-progress.test.ts`). Jika lebih → bug regresi, perbaiki sebelum lanjut.

- [ ] **Step 2: Build produksi**

Run: `pnpm build`
Expected: sukses tanpa error.

- [ ] **Step 3: Checklist QA manual Midtrans sandbox (bantu owner menjalankan)**

1. Login akun pro aktif → `/pricing` → kartu Top Up tampil → beli → bayar di sandbox → kembali: kredit +15, tanggal "Aktif s.d." di `/settings/billing` TIDAK bergeser.
2. Replay payload webhook settlement untuk order top-up yang sama (curl manual) → kredit TETAP +15 sekali.
3. Akun free & akun paused → kartu tidak tampil; POST langsung `/api/payments/create` dengan `{"planId":"topup-15"}` → HTTP 403.
4. Akun Pro borong 2× top-up (30 kredit) → percobaan ke-3 → HTTP 400 pesan kuota habis.
5. Redirect back dari Snap → toast "Berhasil beli kredit…" + saldo navbar ter-update (refetch otomatis).

- [ ] **Step 4: Laporan**

Laporkan hasil verifikasi ke owner beserta catatan deployment: **tanpa migrasi, tanpa env baru — cukup push/deploy aplikasi.** Runbook refund manual ada di spec §9 (kasus sangat jarang: periode expire di antara checkout dan webhook).

