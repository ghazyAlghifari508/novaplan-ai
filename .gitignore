node_modules
.DS_Store
dist
dist-ssr
*.local
# graphify output is generated, never commit
graphify-out/
# scratch screenshots
test-home.png
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# local DB row backups (contain user content)
.backups/

# legacy Claude Code agent config (migrated to .opencode/)
.claude/
