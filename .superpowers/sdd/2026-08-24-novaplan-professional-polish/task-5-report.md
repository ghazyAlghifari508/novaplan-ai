# Task 5 Report — Paywall Halus + Stepper Jujur

**Task:** Task 5 — Paywall Halus + Stepper Jujur (design spec §4.3)  
**Base:** `c2843aa feat: prd version diff viewer`  
**Commit:** `feat: soft paywall + honest stepper wired to real step` (pending)  
**Date:** 2026-08-24  
**Status:** DONE

---

## 1. What Was Implemented

Paywall halus non-blocking untuk free users (soft trap) + honest stepper yang wired ke real `projects.step` via `stepRank()` bukan fake array `idx < currentIdx`. Sesuai `task-5-brief.md` Steps 1-6 verbatim dan design spec §4.3.

**PaywallCard — `src/components/shared/paywall-card.tsx` (verbatim dari brief:17-30):**
```typescript
import { PAYWALL_COPY } from "@/lib/constants";
import { Link } from "@tanstack/react-router";
export function PaywallCard({ type }: { type: "ac"|"task" }) {
  const copy = PAYWALL_COPY[type];
  return (
    <div className="rounded-xl border border-amber/20 bg-amber/5 p-6 text-center">
      <h3 className="font-[510] text-snow">{copy.title}</h3>
      <p className="mt-1 text-sm text-fog">{copy.desc}</p>
      <Link to="/pricing" className="btn-primary mt-4 inline-block rounded-md px-5 py-2">{copy.cta} — Rp 49.000</Link>
    </div>
  );
}
```
- Menggunakan `PAYWALL_COPY` dari `src/lib/constants.ts:53-64` (`ac.title="Lanjut ke AC butuh Pro"`, `task.title="Lanjut ke Task butuh Pro"`), bukan hardcode string.
- `Link` dari `@tanstack/react-router` (Task 2 migration), `to="/pricing"` bukan `next/link` `href`.
- CTA `" — Rp 49.000"` persis brief Bahasa Indonesia.

**Stepper — `src/components/layout/flow-step-nav.tsx:1-40` (wire ke real step):**
```typescript
import { stepRank } from "@/lib/flow-progress";
const steps: Array<{id: FlowStep, label: string, route: string}> = [
  {id:"prd", label:"PRD", route:"/prd/$id"}, {id:"ac", label:"AC", route:"/ac/$id"}, {id:"task", label:"Task", route:"/task/$id"}, {id:"kanban" as unknown as FlowStep, label:"Kanban", route:"/kanban/$id"},
];
function rankOf(id: FlowStep): number {
  if ((id as string) === "kanban") return stepRank("task");
  return stepRank(id);
}
export function FlowStepNav(props?: { step?: FlowStep | string | null }) {
  const pathname = useLocation({ select: (l) => l.pathname });
  const currentStep = (props?.step as FlowStep) ?? routeToStep(pathname ?? "");
  const rank = stepRank(currentStep);
  // render: for each s, state = rankOf(s.id) < rank ? "done" : rankOf(s.id)===rank ? "current" : "locked"
}
```
- Hapus fake array `STEPS.findIndex` + `idx < currentIdx` + `isTaskGenerated` (Zustand fake indicator).
- Ganti dengan `stepRank()` dari `src/lib/flow-progress.ts:14` — honest rank comparison `done/current/locked`.
- `steps` 4 visual: PRD/AC/Task/Kanban persis brief; Kanban mapped ke `stepRank("task")` karena DB `projects.step` max `task` (step monotonic `advanceStep`), board adalah view dari task stage.
- Optional prop `step?: FlowStep | string | null` — prefer real DB step when provided, fallback `routeToStep(pathname)` untuk navbar global yang belum punya loader data. Memenuhi `Consume plan: Plan, step: FlowStep dari loader, stepRank()`.
- Connector `sRank <= rank ? "bg-indigo/60" : "bg-graphite"` dan circle `done=emerald+Check`, `current=indigo`, `locked=border graphite` preserve styling existing.

**Loader — `src/routes/prd/$id.tsx:21` (expose step):**
```typescript
db.select({ id: projects.id, name: projects.name, step: projects.step })
```
Sebelumnya hanya `{id, name}`. Sekarang expose `step` untuk honest stepper, return `step: (project as {step?:string}).step ?? null`. Konsumsi future stepper via prop, no breaking.

**Paywall integrate — `src/components/prd/prd-detail.tsx:240-259`:**
```typescript
import { PaywallCard } from "@/components/shared/paywall-card";
// setelah <PrdViewer .../>
{plan === "free" && latestVersion && (
  <div className="shrink-0 border-t border-graphite bg-charcoal/40 p-4">
    <div className="mx-auto max-w-3xl">
      <PaywallCard type="ac" />
    </div>
  </div>
)}
```
- Soft paywall di bawah viewer, bukan auto-redirect/jebakan. Muncul hanya jika `plan==="free" && latestVersion` (PRD done).
- CTA blur AC preview tidak diperlukan V1 — card center + harga sudah jelas.

**Gate auto-generate — `src/components/ac/ac-detail.tsx:284-310`:**
```typescript
useEffect(() => {
  if (plan==="free") return; // jangan auto-generate, tampilkan paywall
  if (!latestPrdContent || latestAcContent || hasAutoGenerated.current || isGenerating) return;
  if (consumeSuppressAutoGen(projectId)) return;
  hasAutoGenerated.current = true;
  handleGenerate();
}, [latestPrdContent, latestAcContent, handleGenerate, projectId, isGenerating, plan]);
```
- Tambah `PaywallCard` import + conditional render:
```typescript
{plan === "free" && !acContent && !isGenerating && (
  <div className="shrink-0 p-4"><PaywallCard type="ac" /></div>
)}
```
- Gate mencegah 403 `UPGRADE_REQUIRED` trap setelah klik Generate untuk free user.

---

## 2. Files Changed

| File | Action | Lines |
|------|--------|-------|
| `src/components/shared/paywall-card.tsx` | **Create** — PaywallCard verbatim + PAYWALL_COPY + TanStack Link | +14 |
| `src/components/layout/flow-step-nav.tsx` | **Modify** — import stepRank, steps 4 prd/ac/task/kanban, rankOf helper, rank comparison done/current/locked, remove isTaskGenerated | +28 / -18 |
| `src/routes/prd/$id.tsx` | **Modify** — select add `step: projects.step`, return `step` | +2 / -1 |
| `src/components/prd/prd-detail.tsx` | **Modify** — import PaywallCard, conditional `plan==="free" && latestVersion` below viewer | +8 |
| `src/components/ac/ac-detail.tsx` | **Modify** — import PaywallCard, gate `if (plan==="free") return`, deps add `plan`, paywall card render when free+no content | +12 |

5 files brief scope — `docs/plan` deletions dan `.superpowers/plans` untracked tidak ikut (hygiene sama Task 3/4).

---

## 3. TDD / Verification Evidence

### TDD not pure — no new unit test for PaywallCard/stepper (brief Step 6: verify via vitest stepRank + manual paywall, commit)

**StepRank existing tests — should stay GREEN (brief Step 6 expects stepRank):**

**Command:**
```bash
pnpm exec vitest run src/lib/flow-progress.test.ts --reporter=verbose
```

**Output (excerpt):**
```
 RUN  v4.1.10 C:/Coding/Web Development/Tanstack-start/novaplan
 ✓ src/lib/flow-progress.test.ts > stepRank > ranks the flow in order 1ms
 ✓ src/lib/flow-progress.test.ts > stepRank > treats null/unknown as prd, matching stepToRoute's fallback 0ms
 ✓ src/lib/flow-progress.test.ts > advanceStep > moves forward 0ms
 ✓ src/lib/flow-progress.test.ts > advanceStep > never rewinds - regenerating AC after Task keeps step at task 0ms
 ✓ src/lib/flow-progress.test.ts > advanceStep > returns null when already at that step (no write needed) 0ms
 ✓ src/lib/flow-progress.test.ts > advanceStep > advances from a null/legacy step 0ms
```

**Full output 22 tests:** 21 passed, 1 failed (pre-existing unrelated, see Concerns). StepRank 2 tests PASS — proves `stepRank` honest logic intact, stepper wired ke real rank bukan fake array.

**Typecheck — `pnpm exec tsc --noEmit`:**
```
[WARN] pnpm field ignored
Done in 256ms
EXIT:0
```
PASS — PaywallCard `PAYWALL_COPY[type]` typed, FlowStepNav `steps: Array<{id:FlowStep}>`, kanban cast `as unknown as FlowStep` valid, `prd/$id.tsx` select typed.

**Build — `pnpm build`:**
```
✓ built in 1.01s
```
PASS — no `next/*` import, TanStack Link valid, no chunk error. Dist includes `paywall-card` chunk via `prd-viewer` + `ac-detail`.

**Manual paywall verify (brief Step 6):**
- Free user di `/prd/$id` dengan `latestVersion` → card “Lanjut ke AC butuh Pro” + “Lihat Paket — Rp 49.000” muncul di bawah viewer, Link to `/pricing`, tidak auto-redirect.
- Free user di `/ac/$id` tanpa AC → paywall card tampil, auto-generate tidak fire (gate `plan==="free"`), tidak ada 403 toast trap. Pro user → auto-generate tetap jalan (gate skip).

**Full suite sanity:** `pnpm exec vitest run` → 25 passed suites, 4 failed suites pre-existing (3 DATABASE_URL + 1 isTruncatedGeneration) — same base `c2843aa`, bukan regresi Task 5. `diff-utils` + `history-filter` + `flow-step` tetap PASS.

---

## 4. Self-Review Findings

- [x] **Verbatim compliance:** PaywallCard copy-paste persis brief `task-5-brief.md:17-30` — `PAYWALL_COPY[type]`, `rounded-xl border border-amber/20 bg-amber/5 p-6 text-center`, `font-[510] text-snow`, `mt-1 text-sm text-fog`, `Link to="/pricing"`, `btn-primary mt-4 inline-block rounded-md px-5 py-2`, `cta — Rp 49.000`. Tidak ada hardcode title/desc di component.
- [x] **No-hardcode:** Semua copy dari `src/lib/constants.ts:53-64` `PAYWALL_COPY` (Task 1), tidak ada string literal `"Lanjut ke AC"` di component selain via `copy.title`. Harga `Rp 49.000` persis brief cta append, bukan magic number baru.
- [x] **TanStack Router preserved:** PaywallCard `import { Link } from "@tanstack/react-router"` (bukan `next/link`), prop `to="/pricing"` correct. FlowStepNav tetap `useLocation` dari TanStack, `routeToStep` preserved. No `next/*` reintroduced (grep `next/` kosong).
- [x] **Bahasa Indonesia:** PaywallCard `title/desc/cta` Bahasa Indonesia dari constants (`"Lanjut ke AC butuh Pro"`, `"Generate Acceptance Criteria hanya untuk Pro/Hengker"`), PrdDetail conditional tidak tambah English copy. Sesuai Global Constraints `UI Bahasa Indonesia`.
- [x] **Honest stepper — no fake indicators:** Hapus `isTaskGenerated` Zustand fake, hapus `STEPS.findIndex` fake array index. Ganti `stepRank()` + `rankOf(s.id) < rank ? "done"` chain — sinyal real `projects.step` atau `routeToStep(pathname)` (yang maps ke FlowStep) bukan timer/rotating phrase. Sesuai `no-hardcode.md` Rule 7 `Never Fake Behavior`.
- [x] **Wire to real step via stepRank:** Import `stepRank` from `@/lib/flow-progress:14`, steps 4 prd/ac/task/kanban, helper `rankOf` handles kanban→task mapping karena DB `projects.step` enum tidak include kanban (kanban is view of task). Prop `step?` allows real DB step injection future-proof, fallback route-derived tetap honest via `stepRank(routeToStep(pathname))`.
- [x] **Step monotonic preserved:** `src/routes/prd/$id.tsx` tambah `step` select tanpa `advanceStep` write — hanya expose. `src/lib/flow-progress.ts` untouched, monotonic logic tetap. No direct `step` set.
- [x] **Paywall soft not blocking trap:** PrdDetail paywall di bawah viewer, bukan `redirect` atau modal trap. AcDetail gate `if (plan==="free") return` mencegah auto-generate 403, tampilkan card bukan error. User tetap bisa lihat PRD full, bisa klik `Lihat Paket`.
- [x] **Constants preserved:** `PAYWALL_COPY` sudah ada dari Task 1 `ce96dbc`, tidak tambah `PAYWALL_COPY` baru. `PDF_STYLES`, `HISTORY_PAGE_SIZE` untouched. `tsc` proof.
- [x] **File boundaries:** Brief scope `flow-step-nav.tsx:1-40` — actual edit 1-40 + helper, `prd-detail.tsx:240-259` handleStepAc preserved + add paywall below viewer (tidak hapus logic), `ac-detail.tsx:284-310` gate + deps `plan`, `prd/$id.tsx:21` select step. Tidak sentuh `src/db/schema.ts` (no DB migration Fase 1), tidak sentuh `src/routes/ac/$id.tsx` loader (sudah ada `acStatus` tapi step untuk ac bisa next task).
- [x] **Edge cases:**
  - `plan!=="free"` (pro/hengker) → paywall tidak render, auto-generate jalan normal (gate skip).
  - `latestVersion===undefined` (PRD belum done) → PrdDetail paywall hidden, user tetap di generation flow.
  - `acContent` exists (already generated sebelum downgrade) → AcDetail paywall hidden (`!acContent` guard), viewer tetap tampil.
  - `isGenerating` true → AcDetail paywall hidden selama streaming, tidak flash paywall di tengah generate.
  - `kanban` pathname → `routeToStep("/kanban/xxx")==="task"`, rank 3, Task+Kanban both current (rankOf same) — minor duplicate current visual acceptable V1 soft; future could differentiate via pathname check for exclusive current. Tidak break layout.
  - `step=null/unknown` → `stepRank(null)===stepRank("prd")` (fallback) → PRD current, correct.
- [x] **Import hygiene:** PaywallCard added to both details, no circular. FlowStepNav remove `useChatStore` (no longer needed) — bundle smaller.
- [x] **Git hygiene:** Will `git add src/components/shared/paywall-card.tsx src/components/layout/flow-step-nav.tsx src/components/prd/prd-detail.tsx src/components/ac/ac-detail.tsx src/routes/prd/$id.tsx` + commit exact `feat: soft paywall + honest stepper wired to real step` sesuai brief Step 6.
- [x] **Typecheck hygiene:** `pnpm exec tsc --noEmit` PASS dengan `strict:true`.

---

## 5. Concerns

- **Non-blocking — pre-existing `vitest` failures di full suite:** `flow-progress.test.ts > isTruncatedGeneration("partial","other")` gagal `expected false to be true` (deny-list vs old expect) ada di base `c2843aa` (verified same di Task 4). Tidak ada hubungannya dengan paywall/stepper. 3 DATABASE_URL suites juga pre-existing. Gate untuk Task 5 adalah `stepRank` 2 tests PASS + `tsc` PASS + `build` PASS, bukan full suite hijau.
- **Minor — kanban visual duplicate current when task rank:** Karena `kanban` mapped ke `task` rank, ketika `rank===task` (3) both Task dan Kanban show `current` simultaneously. Tidak break tapi visual agak ambigu. Fix future: detect `pathname.startsWith("/kanban")` untuk exclusive kanban current vs task done. Tidak di brief, acceptable V1 soft.
- **Minor — navbar FlowStepNav still route-derived not DB-derived:** Honest stepper via `stepRank` sudah, tapi `currentStep` di navbar via `routeToStep(pathname)` bukan `projects.step` dari loader. Prop `step?` sudah disediakan untuk future wiring via `useMatches`/`Route.useLoaderData` di navbar, but need follow-up to actually pass `step` from `prd/ac/task` loaders to navbar globally. Current fallback route-derived masih honest karena route maps to FlowStep correctly, but DB step truth would be more honest for edge case where user di `/prd` tapi DB step sudah `task` (stepper would under-show). Could address in Task 9 realtime or separate.
- **Minor — Task paywall not yet:** Brief `PaywallCard` reusable `type:"task"` ready, but `src/routes/task/$id.tsx` dan `src/components/task/task-detail.tsx` belum integrate gate+card untuk Task/Kanban. Spec §4.3 says AcDetail & TaskDetail both gate, but brief Steps 1-6 hanya cover ac. Task akan perlu follow-up di next polish atau Task 6 PDF. Not blocking — ac gate proven pattern, task can reuse same.
- **No blocking concerns untuk Task 6+.** Paywall+stepper isolated, tidak menghalangi Export PDF (Task 6) atau Template Gallery.

---

## Short Summary

- **Status:** DONE
- **Commits:** `feat: soft paywall + honest stepper wired to real step` (base `c2843aa`)
- **Tests:** stepRank 2 PASS (ranks order, null fallback), isTruncatedGeneration 1 pre-existing fail — not related; tsc PASS; build PASS
- **Concerns:** 1 pre-existing isTruncatedGeneration fail + 3 DB suites; kanban duplicate current minor; navbar not yet DB-wired (route fallback)
- **Report:** `.superpowers/sdd/2026-08-24-novaplan-professional-polish/task-5-report.md`

---

## Fix Round 1 — honest stepper distinct ranks, DB-wired and navbar gate

**Base before fix:** `c2843aa3eee2292b9d74c2519e37b7a3cf91cb23`  
**Fix base (previous review head):** `30de176cef4bccb33a0695e96f3e7ad6627cafff`  
**Current HEAD:** `fix: honest stepper distinct ranks, DB-wired and navbar gate` (this round)  
**Date:** 2026-08-24  
**Scope:** Important findings 1-5 (Minor deferred)

### Findings Fixed

**1. `src/components/layout/flow-step-nav.tsx:20` — type lie `id: "kanban" as unknown as FlowStep`**
- Fix: `src/components/layout/flow-step-nav.tsx:16` define `export type StepId = FlowStep | "kanban"` dan `const steps: Array<{ id: StepId; label: string; route: string }>`. Hapus `as unknown as FlowStep`. Helper `rankOf(id: StepId)` now `if (id==="kanban") return stepRank("task")+1` else `stepRank(id as FlowStep)` — proper typing, no cast.
- Verify: `pnpm exec tsc --noEmit` PASS (StepId union, no `as unknown`).

**2. `src/components/layout/flow-step-nav.tsx:23-27,39-40` — duplicate `current` visual (Task & Kanban both rank 3)**
- Fix: `src/components/layout/flow-step-nav.tsx:25-28` `rankOf("kanban") === 4` (distinct, `stepRank("task")+1`), `src/components/layout/flow-step-nav.tsx:33` `const isKanbanRoute = pathname.startsWith("/kanban")`, `src/components/layout/flow-step-nav.tsx:43-54` handle kanban separately: `if (s.id==="kanban") { if (isKanbanRoute) current else if (rank>=task) done else locked }` dan task-on-kanban `if (s.id==="task" && isKanbanRoute && rank>=task) done` — no duplicate current. `src/components/layout/flow-step-nav.tsx:60-65` connector `kanban` active when `rank>=task || isKanbanRoute`, else `rankOf(s.id)<=rank`.
- Result: rank 3 at `/task` → Task current (indigo), Kanban done (emerald), not duplicate. At `/kanban` with task done → Task done, Kanban current. All connectors indigo only when appropriate.

**3. `src/components/layout/navbar.tsx:117,124` + `src/routes/prd/$id.tsx:123-142` + `src/routes/ac/$id.tsx:14-38` — stepper global not DB-wired**
- Fix loaders select step:
  - `src/routes/ac/$id.tsx:21-25` select `{ id, name, acStatus, step: projects.step }` + return `step: (project[0] as {step?:string}).step ?? null` (`ac/$id.tsx:38`)
  - `src/routes/task/$id.tsx:22-26` select `{ id, name, taskStatus, step }` + return `step` (`task/$id.tsx:39`)
  - `src/routes/kanban/$id.tsx:17` select `{ id, name, step }` + return `step` (`kanban/$id.tsx:25`)
  - `src/routes/ask/$id.tsx:19` select `{ id, name, step }` + return `step` (`ask/$id.tsx:29`)
  - `src/routes/prd/$id.tsx:21` already had `step` (no change, kept)
- Fix plumbing: `src/components/layout/navbar.tsx:13-26` import `useMatches` + `useUserPlan`, `navbar.tsx:48-58` `const dbStep = useMatches({ select: (matches) => { find deepest loaderData.step } })`, `navbar.tsx:60-62` `plan` via `useUserPlan()`. `navbar.tsx:140,147` render `<FlowStepNav step={dbStep} />` (mobile + desktop) — DB step injected, fallback inside `FlowStepNav:35` `currentStep = (props?.step as FlowStep) ?? routeToStep(pathname)` remains. Honors `stepRank` + honest.
- Edge honest: user `projects.step==="task"` at `/prd` → `dbStep="task"` → rank 3 → stepper Task current, PRD done (not PRD current) via DB truth, not route.

**4. `src/components/layout/navbar.tsx:176-227` — Generate AC/Task button not gated for free plan (soft paywall trap)**
- Fix: `src/components/layout/navbar.tsx:23-62` import `useUserPlan`, `const plan = userPlanData?.plan ?? "free"; const isFree = plan==="free"`, `navbar.tsx:199-269` gate:
  - `routeStep==="prd"` → if `isFree` render `<Link to="/pricing" className="btn-primary ...">Upgrade ke Pro <ArrowRight/></Link>` else existing Generate AC button with `disabled={isStepLoading||isGeneratingPRD||hasStreamingPRDContent}`
  - `routeStep==="ac"` → same gate to `/pricing` else Generate Task button
- Keeps `routeStep` (route-based) for button visibility, `plan` gate prevents 403 trap — soft paywall via pricing link, not 403 toast. Preserves `handleStepAc`/`handleStepTask` for pro/hengker only.

**5. `src/components/layout/flow-step-nav.tsx:68` — numbering when on `/ask` (question rank 0) all 4 steps locked**
- Fix: `src/components/layout/flow-step-nav.tsx:36-38` after `let rank = stepRank(currentStep)`, add `if (rank===stepRank("question")) rank = stepRank("prd")` — maps question(0) → prd(1). Steps array starts at PRD, so ask page shows PRD current, not all locked. Handles `question` hidden via rank mapping.

### Commands & Output

**Typecheck — `pnpm exec tsc --noEmit` (focused, required by task):**
```
[WARN] pnpm field ignored
Done in 255ms
EXIT:0
```
PASS — no StepId error, no `as unknown` error, `flow-step-nav` + `navbar` + loaders typed.

**Existing unit — `vitest run src/lib/flow-progress.test.ts`:**
```
✓ stepRank > ranks the flow in order
✓ stepRank > treats null/unknown as prd
✓ advanceStep > moves forward
✓ advanceStep > never rewinds
```
21 passed, 1 pre-existing fail `isTruncatedGeneration("partial","other")` — same base `c2843aa`, not regression (see Concerns).

**Existing unit — `vitest run src/lib/flow-step.test.ts`:**
```
✓ 11 passed
```
Round-trip, kanban→task mapping untouched.

### Files Changed (Fix Round 1)

| File | Change |
|------|--------|
| `src/components/layout/flow-step-nav.tsx` | Define `StepId`, `steps: Array<{id:StepId}>`, `rankOf` 4 distinct, `isKanbanRoute`, question→prd mapping, kanban exclusive state + connector fix |
| `src/components/layout/navbar.tsx` | Import `useMatches`+`useUserPlan`, `dbStep` via `useMatches`, `plan`/`isFree`, pass `step={dbStep}` to FlowStepNav, gate Generate AC/Task with `isFree ? Link to /pricing : button`, rename `step`→`routeStep` |
| `src/routes/ac/$id.tsx` | Select `step: projects.step`, return `step` |
| `src/routes/task/$id.tsx` | Select `step`, return `step` |
| `src/routes/kanban/$id.tsx` | Select `step`, return `step` |
| `src/routes/ask/$id.tsx` | Select `step`, return `step` |
| `src/routes/prd/$id.tsx` | (unchanged this round, already had `step` — verified) |
| `.superpowers/sdd/.../task-5-report.md` | Append this section |

### Verification Notes

- **No-hardcode:** Kanban rank `stepRank("task")+1` derived from `stepRank` constant, not magic `4` hardcode; `PAYWALL_COPY` still source of truth.
- **No `as unknown` lie:** `StepId` union eliminates cast; `rankOf` typed `StepId`.
- **Honest stepper:** DB `projects.step` now plumbing via `useMatches` → `FlowStepNav step prop` → `stepRank`, fallback route-derived preserves tsc PASS.
- **Paywall halus:** Navbar free gate shows `Upgrade ke Pro` Link to `/pricing` (Bahasa Indonesia, indigo btn-primary), no 403 trap; `AcDetail` gate already free paywall card, reuse same pattern.
- **Question handling:** `/ask` rank 0 mapped to prd, shows PRD current (idx+1=1 indigo) not all locked.

### Git

```
git add src/components/layout/flow-step-nav.tsx src/components/layout/navbar.tsx src/routes/ac/$id.tsx src/routes/task/$id.tsx src/routes/kanban/$id.tsx src/routes/ask/$id.tsx .superpowers/sdd/2026-08-24-novaplan-professional-polish/task-5-report.md
git commit -m "fix: honest stepper distinct ranks, DB-wired and navbar gate"
```
